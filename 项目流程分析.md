# PentestAgent 自动化渗透测试流程详细分析

## 项目概述

**PentestAgent** 是一个基于大语言模型（LLM）驱动的自动化渗透测试框架，通过三个核心代理（Agent）实现从信息收集、漏洞分析到漏洞利用的完整自动化流程。

## 核心架构

项目采用**三阶段代理架构**：

1. **Reconnaissance Agent（侦察代理）** - 信息收集阶段
2. **Planning Agent（规划代理）** - 漏洞分析与规划阶段  
3. **Execution Agent（执行代理）** - 漏洞利用执行阶段

---

## 详细流程分析

### 阶段一：Reconnaissance Agent（侦察代理）

**文件位置**: `agents/recon_agent_new.py`

#### 工作流程

1. **初始化阶段**
   - 从 `config.yaml` 读取配置（目标IP、模型、主题等）
   - 初始化 LangChain 1.0 模型（支持 OpenAI、DeepSeek 等）
   - 创建带历史记录管理的对话链（`RunnableWithMessageHistory`）
   - 设置文件聊天历史存储（`FileChatMessageHistory`）

2. **侦察循环（最多10步）**
   ```
   for step in range(max_steps):
       ├─ LLM 分析当前状态
       ├─ 生成下一步行动（analysis, next_step, executable）
       ├─ 如果 executable != 'None':
       │   ├─ 执行 shell 命令（如 nmap、nmap -p- 等）
       │   └─ 将命令结果反馈给 LLM
       └─ 否则：进入总结阶段
   ```

3. **关键功能**
   - **命令执行**: 通过 `run_shell_command()` 执行渗透测试工具（nmap、masscan 等）
   - **智能分析**: LLM 根据命令输出决定下一步行动
   - **历史管理**: 自动保存对话历史到 `recon_memory/{topic}.json`

4. **输出结果**
   - 系统信息（操作系统、开放端口、服务版本等）
   - 网络拓扑信息
   - 最终侦察总结报告

#### 调用示例
```python
# 配置示例（config.yaml）
runtime:
  recon:
    current_topic: "Apache ActiveMQ"
    target_ip: "192.168.11.130"
    model: "gpt-4o-mini"
    memory_dir: "recon_memory"
```

---

### 阶段二：Planning Agent（规划代理）

**文件位置**: `agents/planning_agent_new.py`

#### 工作流程

1. **CVE 发现阶段**
   ```
   cvemap_product(app_name)
   ├─ 调用 vulnx 工具搜索产品相关 CVE
   ├─ 过滤 CVE（年份范围、最大条目数）
   └─ 返回 CVE 列表
   ```

2. **版本过滤（可选）**
   ```
   get_affected_cve(cve_list, version)
   └─ 根据目标版本过滤相关 CVE
   ```

3. **漏洞分析阶段** (`utils/cve_info.py` 的 `get_exp_info()`)
   
   **3.1 CVE 详细信息获取**
   ```
   cvemap_search(cve)
   ├─ 调用 vulnx 获取 CVE 详细信息
   └─ 返回 CVE 描述、CVSS 分数、EPSS 分数等
   ```

   **3.2 多源漏洞利用代码搜索**
   ```
   compose(output_dir, cve)
   ├─ GitHub 搜索（GithubSearcher）
   │   ├─ 使用 GitHub API 搜索相关仓库
   │   ├─ 过滤低质量仓库（评分机制）
   │   └─ 克隆相关仓库到本地
   ├─ ExploitDB 搜索（ExploitDBSearcher）
   │   ├─ 使用 searchsploit 本地搜索
   │   └─ 复制相关 exploit 文件
   └─ Google 搜索（GoogleSearcher）
       └─ 搜索相关文档和 PoC
   ```

   **3.3 漏洞特征提取** (`utils/doc_handler.py`)
   ```
   DocHandler.vul_analysis(cve, output_dir, description)
   ├─ 代码分析（ExploitDB + GitHub）
   │   ├─ 语言类型识别（judge_class）
   │   ├─ 漏洞类型分类（Code Execution/Privilege Escalation/等）
   │   ├─ 利用成熟度（Exploit/PoC/None）
   │   ├─ 远程利用性（isRemote）
   │   └─ 攻击复杂度分析（6个维度）
   └─ 文档分析（Google 搜索结果）
       ├─ 漏洞类型分类
       ├─ 远程利用性
       └─ 攻击复杂度
   ```

   **3.4 CVE 评分与分类** (`utils/cve_info.py` 的 `cve_classifier()`)
   ```
   calculate_score(features, trending_score)
   ├─ 根据配置权重计算分数：
   │   ├─ 漏洞类型权重（Code Execution: 10, Privilege Escalation: 8, ...）
   │   ├─ 利用成熟度权重（Exploit: 1.0, PoC: 0.6, None: 0.3）
   │   ├─ 远程利用性权重（True: 20, False: 0）
   │   ├─ 攻击复杂度权重（多个维度，每个 +5 或 0）
   │   ├─ 语言分类权重（c1/c2/c3）
   │   └─ 来源权重（ExploitDB: 1.1, GitHub: 1.0, Google: 0.85）
   ├─ 计算最终分数
   └─ 分类为 easy/medium/hard
   ```

4. **结果合并** (`utils/merge_scores.py`)
   ```
   merge(root_directory, output_json)
   ├─ 遍历所有 CVE 的 classification.json
   ├─ 提取所有 exploit 的评分
   ├─ 按分数降序排序
   └─ 生成 plan.json（包含所有 exploit 路径和分数）
   ```

5. **AI 总结生成**
   ```
   LLM 分析 plan.json
   ├─ 生成漏洞利用计划摘要
   ├─ 推荐最优先的 CVE
   └─ 提供下一步行动建议
   ```

#### 关键配置文件
```yaml
runtime:
  planning:
    model: "gpt-4o-mini"
    keyword: "ActiveMQ"
    app: "ActiveMQ"
    version: "5.17.3"
    vuln_type: "Deserialization RCE"
    output_dir: "../data/exp_info"
    cvemap:
      fuzzy_search: True
      max_year: 2023
      min_year: 2023
      max_entry: 60
```

---

### 阶段三：Execution Agent（执行代理）

**文件位置**: `agents/execution_agent_new.py`

#### 工作流程

1. **初始化阶段**
   ```
   ExecutionAgent.__init__(doc_dir)
   ├─ 克隆 exploit 仓库（如果提供 repo_url）
   ├─ 初始化 LLM 模型
   ├─ 设置工具（Tools）：
   │   ├─ RAG 检索工具（query_exploit_documentation）
   │   │   ├─ 使用 FAISS 向量数据库
   │   │   ├─ 加载或创建文档索引
   │   │   └─ 支持语义搜索 exploit 文档
   │   └─ Shell 命令执行工具（run_shell_command）
   │       └─ 在指定目录执行命令
   └─ 创建 Agent Executor（带工具调用能力）
   ```

2. **初始分析阶段**
   ```
   exec_agent.invoke("分析 exploit 文档...")
   ├─ 使用 RAG 工具查询 exploit 文档
   ├─ 理解 exploit 的工作原理
   └─ 识别需要的信息（目标IP、端口、命令等）
   ```

3. **可选：与 Recon Agent 交互**
   ```
   if current_topic:
       recon_agent = ReconAgent()
       recon_res = recon_agent.run(topic, query_msg)
       └─ 获取缺失的侦察信息
   ```

4. **执行阶段**
   ```
   exec_agent.invoke(final_guide_prompt)
   ├─ Agent 自主决策：
   │   ├─ 分析 exploit 代码
   │   ├─ 检查依赖和环境
   │   ├─ 执行必要的安装/配置步骤
   │   ├─ 修改 exploit 代码（如需要）
   │   ├─ 运行 exploit
   │   └─ 验证利用结果
   └─ 使用工具：
       ├─ query_exploit_documentation: 查询文档
       └─ run_shell_command: 执行命令
   ```

5. **总结阶段**
   ```
   exec_agent.invoke("生成执行总结...")
   └─ 生成执行报告
   ```

#### 关键配置
```yaml
runtime:
  execution:
    current_topic: "Apache ActiveMQ OpenWire Protocol Deserialization RCE"
    repo_url: "https://github.com/X1r0z/ActiveMQ-RCE.git"
    doc_dir: "../data/exp_source"
    target_ip: "192.168.11.130"
    target_port: "61616"
    attacker_ip: "127.0.0.1"
    command_to_execute: "touch /tmp/success"
    model: "gpt-4o-mini"
```

---

## 完整调用链路图

```
用户启动
    │
    ├─→ [1] Reconnaissance Agent
    │       │
    │       ├─ 读取配置（target_ip, model）
    │       ├─ 初始化 LLM + 历史管理
    │       ├─ 循环执行：
    │       │   ├─ LLM 分析 → 生成命令
    │       │   ├─ 执行命令（nmap 等）
    │       │   └─ 反馈结果 → 继续分析
    │       └─ 生成侦察报告
    │           │
    │           └─→ 保存到 recon_memory/{topic}.json
    │
    ├─→ [2] Planning Agent
    │       │
    │       ├─ 读取配置（app, version, keyword）
    │       ├─ CVE 发现：
    │       │   └─ vulnx search -p {app}
    │       │
    │       ├─ 版本过滤（可选）：
    │       │   └─ get_affected_cve()
    │       │
    │       ├─ 对每个 CVE：
    │       │   ├─ cvemap_search() → 获取 CVE 详情
    │       │   ├─ compose() → 多源搜索：
    │       │   │   ├─ GitHub 搜索 → 克隆仓库
    │       │   │   ├─ ExploitDB 搜索 → 复制文件
    │       │   │   └─ Google 搜索 → 保存文档
    │       │   │
    │       │   ├─ DocHandler.vul_analysis() → 特征提取：
    │       │   │   ├─ 代码分析（语言、漏洞类型、成熟度等）
    │       │   │   └─ 文档分析（漏洞类型、复杂度等）
    │       │   │
    │       │   └─ cve_classifier() → 评分分类：
    │       │       ├─ calculate_score() → 计算分数
    │       │       └─ 分类为 easy/medium/hard
    │       │
    │       ├─ merge() → 合并所有结果
    │       │   └─ 生成 plan.json（按分数排序）
    │       │
    │       └─ LLM 总结 → AI 生成利用计划
    │           │
    │           └─→ 保存到 data/exp_info/{keyword}/plan.json
    │
    └─→ [3] Execution Agent
            │
            ├─ 读取配置（repo_url, target_ip, target_port）
            ├─ 克隆 exploit 仓库（如果提供）
            ├─ 初始化 RAG 系统：
            │   ├─ 加载/创建 FAISS 向量索引
            │   └─ 设置工具（RAG + Shell）
            │
            ├─ 初始分析：
            │   └─ RAG 查询 → 理解 exploit
            │
            ├─ 可选：调用 Recon Agent
            │   └─ 获取缺失信息
            │
            ├─ 执行阶段：
            │   ├─ Agent 自主决策
            │   ├─ 使用工具：
            │   │   ├─ query_exploit_documentation
            │   │   └─ run_shell_command
            │   └─ 执行 exploit → 验证结果
            │
            └─ 生成执行总结
                │
                └─→ 保存到 execution_agent_v2.log
```

---

## 关键技术点

### 1. LLM 集成
- **LangChain 1.0**: 使用 LCEL（LangChain Expression Language）构建链式调用
- **结构化输出**: 使用 Pydantic 模型确保 LLM 输出格式正确
- **历史管理**: `RunnableWithMessageHistory` 自动管理对话历史

### 2. RAG（检索增强生成）
- **向量数据库**: FAISS 存储 exploit 文档的向量表示
- **文档分割**: `RecursiveCharacterTextSplitter` 处理长文档
- **语义搜索**: OpenAI Embeddings 实现语义相似度搜索

### 3. 多源情报聚合
- **GitHub**: API 搜索 + 仓库克隆
- **ExploitDB**: 本地 searchsploit 工具
- **Google**: 网页搜索 + 文档提取
- **vulnx**: CVE 详细信息获取

### 4. 评分系统
- **多维度评分**: 漏洞类型、利用成熟度、远程利用性、攻击复杂度等
- **权重配置**: 通过 `config.yaml` 灵活配置各维度权重
- **来源权重**: 不同来源（ExploitDB/GitHub/Google）有不同的可信度权重

### 5. 工具调用（Tool Calling）
- **Agent Executor**: LangChain 的 Agent 框架
- **工具定义**: 使用 `@tool` 装饰器定义工具
- **自主决策**: Agent 根据上下文自主选择工具

---

## 数据流转

```
配置（config.yaml）
    ↓
Recon Agent → 侦察数据（recon_memory/{topic}.json）
    ↓
Planning Agent → CVE 列表 + Exploit 信息（data/exp_info/{keyword}/）
    ↓
Execution Agent → Exploit 仓库（data/exp_source/{repo}/）
    ↓
执行结果（日志 + 验证命令输出）
```

---

## 依赖工具

1. **vulnx**: CVE 信息查询工具
2. **searchsploit**: ExploitDB 本地搜索工具
3. **nmap/masscan**: 端口扫描工具
4. **Git**: 仓库克隆工具
5. **Python 包**: LangChain, LlamaIndex, FAISS, OpenAI SDK 等

---

## 总结

PentestAgent 通过三个代理的协作，实现了从信息收集到漏洞利用的完整自动化流程：

1. **Recon Agent** 负责主动侦察，使用 LLM 智能决策执行哪些命令
2. **Planning Agent** 负责漏洞发现和分析，通过多源搜索和智能评分找到最佳利用路径
3. **Execution Agent** 负责实际执行，使用 RAG 理解 exploit 代码并自主执行

整个系统充分利用了 LLM 的推理能力和工具调用能力，实现了高度自动化的渗透测试流程。

